<Page
    x:Class="PayrollApp.Views.AppInitPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PayrollApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Left"
    Height="740" Width="540"
    Loaded="Page_Loaded"
    Background="{ThemeResource SystemControlAcrylicElementBrush}">

    <Grid x:Name="rootGrid" CornerRadius="3">
        <TextBlock Text="apSHA" Style="{ThemeResource HeaderTextBlockStyle}" 
                       Margin="0,192,0,0" HorizontalAlignment="Center" VerticalAlignment="Top"/>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,120">
            <ProgressRing IsActive="True" Height="32" Width="32" Foreground="White"/>
            <TextBlock x:Name="loadText" Text="Connecting to database..." Style="{ThemeResource SubtitleTextBlockStyle}"
                       FontWeight="Light" Margin="0,8,0,0" TextAlignment="Center"/>
        </StackPanel>

    </Grid>
</Page>
